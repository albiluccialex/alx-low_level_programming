#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * int_strlen- Entry point
 *
 * Description: Returns the length of a string
 *
 * Return: 0
 */
int _strlen(char *s)

{
	strlen(str1);
}
