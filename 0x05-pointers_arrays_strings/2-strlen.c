#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/**
 * _strlen- Will return length of string.
 *
 * Description: Returns the length of a string
 *
 * Return: 0
 */

int _strlen(char *s)

{
	strlen (char *s);

	return (0);
}
